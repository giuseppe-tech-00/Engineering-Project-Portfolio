# publications/

Drop your PDF files here and update the links in `index.html`.

Example filenames:
- `thesis.pdf`
- `paper-2023.pdf`
- `paper-2022.pdf`

Then in `index.html`, update the `href` attributes on the `.pub-link` elements
inside `<section id="publications">`.
